{
  "name": "marc-jsons",
  "version": "1.0.0",
  "description": "Generate visual style guides in Figma from markdown or JSON design tokens",
  "scripts": {
    "build": "node esbuild.js",
    "watch": "node esbuild.js --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
